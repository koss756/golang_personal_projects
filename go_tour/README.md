### SLices

slices are dynamically sized data structures that represent a segment of a fixed sized array. Slices do not store data itself but are a reference to a subsection of an array

You can create a slice literal by not mentioning the length
